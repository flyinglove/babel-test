{
  "name": "page-experience-suite",
  "private": true,
  "version": "1.0.0",
  "workspaces": [
    "admin",
    "renderer"
  ],
  "scripts": {
    "dev:admin": "yarn --cwd admin dev",
    "build:admin": "yarn --cwd admin build",
    "preview:admin": "yarn --cwd admin preview",
    "dev:renderer": "yarn --cwd renderer dev",
    "build:renderer": "yarn --cwd renderer build",
    "preview:renderer": "yarn --cwd renderer preview"
  }
}
